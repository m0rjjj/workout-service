import { expect } from 'chai';
import { ExerciseLogCtrl } from './ExerciseLogCtrl';

describe('ExerciseLogCtrl', () => {
  let ctrl;
  beforeEach(() => {
    ctrl = new ExerciseLogCtrl();
  });

  it('should return exercise log list', () => {
    expect(true).to.be.true;
  });
});